@model  IEnumerable<WebsiteDatXeCongNghe.Models.TaiXe>
@{
    ViewBag.Title = "TaiXeDonKhach";
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Đã nhận chuyến</title>

    <link href="~/css/TaiXeDonKhach.css" rel="stylesheet" />
    <link href="~/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">

    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="~/Scripts/jquery.signalR-2.4.3.min.js"></script>
    <script src="~/signalr/hubs"></script>
    <link href="~/css/TaiXe.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.0.19/dist/sweetalert2.all.min.js"></script>


    <script src="~/js/bootstrap.min.js"></script>
    <script src="~/js/NhanChuyenXe.js"></script>
    <link href="~/css/NhanTin.css" rel="stylesheet" />

    <script src="~/js/ie10-viewport-bug-workaround.js"></script>
    <link href="https://fonts.googleapis.com/css?family=Poppins:500&display=swap" rel="stylesheet">
    <link href="//netdna.bootstrapcdn.com/bootstrap/3.0.0/css/bootstrap-glyphicons.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <!-- IE10 viewport hack for Surface/desktop Windows 8 bug -->
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->

    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDM4R4ZQGmaCj3wA4R3ykoEr4EeQPHSvRc&libraries=places"></script>




    <script src="~/js/TaiXeGoogleMap.js"></script>
    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
</head>
<body>
    <div class="header">
        <h1>Đã nhận chuyến, di chuyển thôi!</h1>

    </div>

    <span id="phoneNumbers">@Session["SoDienThoai"].ToString()</span>
    @foreach (var tx in Model)
    {
        <span id="DriverLocations">Vị trí: @tx.ViTri</span>
        <span id="DriverNames">Họ tên: @tx.Ten</span>
        <img src="~/image/TaiXe/@tx.HinhAnh" alt="" width="50" height="50" id="DriverImages">


        <table>
            <tr>
                <td>Mức độ đánh giá:</td>
                <td>
                    <div class="star-ratings" id="DriverRates">
                        @for (int i = 0; i < @tx.MucDoDanhGiaTB; i++)
                        {
                            <i class="fas fa-star"></i>
                        }
                    </div>
                    <span id="DriverRated" style="display:none;">@tx.MucDoDanhGiaTB</span>
                </td>
            </tr>
        </table>
        <span id="DriverLicenses">Biển số xe: @tx.BienSo</span>
        <div id="locations"></div>
    }
    <section id="map">
        <!-- Map will be displayed here -->
        <div class="col-md-8 google-map-wrap">
            <div id="googleMap" class="map"></div>
        </div>

    </section>
    @*<section id="ride-details">
            <h2>Ride details</h2>
            <p>Distance: <span id="distances"></span></p>
            <p>Duration: <span id="durations"></span></p>
            <p>Total Fare: <span id="totalFares">abc</span></p>
            <p>Pickup location: <span id="txtLocationPickups">abc</span></p>
            <p>Dropoff location: <span id="txtLocationDrops">abc</span></p>
            <p>Booking type: <span id="types"></span></p>
            <p>Passenger phone: <span id="phone-numbers"></span></p>
            <p>Payment method: <span id="results">abc</span></p>
            <p>Booking time: <span id="dateds"></span></p>
            <button id="accept-ride">Accept Ride</button>
            <button id="cancel-ride" style="display:none;">Cancel Ride</button>
        </section>*@
    <div id="driver-info">
        <div class="driver-image" id="DriverImage">
            <img src="" alt="Driver Image" width="50" height="50">
        </div>
        <div class="driver-image" id="PassengerImages">
            <img src="" alt="Driver Image" width="50" height="50">
        </div>
        <table>
            <tr>
                <td>Tên tài xế:</td>
                <td><span class="driver-name" id="DriverName"></span></td>
            </tr>
            <tr>
                <td>Số điện thoại tài xế:</td>
                <td><span class="driver-name" id="phoneNumber"></span></td>
            </tr>
            <tr>
                <td>Số điện thoại khách hàng:</td>
                <td><span class="driver-name" id="phone-number"></span></td>
            </tr>
            <tr>
                <td>Tên khách hàng:</td>
                <td><span class="driver-name" id="PassengerName"></span></td>
            </tr>
            <tr>
                <td>Giấy phép lái xe:</td>
                <td><span class="driver-license" id="DriverLicense"></span></td>
            </tr>
            <tr>
                <td>Đánh giá:</td>
                <td>
                    <div class="star-ratings" id="DriverRate">
                        <span class="fa fa-star"></span>
                        <span class="fa fa-star"></span>
                        <span class="fa fa-star"></span>
                        <span class="fa fa-star"></span>
                        <span class="fa fa-star-o"></span>
                    </div>
                </td>
            </tr>
            <tr>
                <td>Vị trí tài xế:</td>
                <td><span class="pickup-point" id="DriverLocation"></span></td>
            </tr>
            <tr>
                <td>Điểm đón:</td>
                <td><span class="pickup-point" id="txtLocationPickup"></span></td>
            </tr>
            <tr>
                <td>Điểm đến:</td>
                <td><span class="destination" id="txtLocationDrop"></span></td>
            </tr>
            <tr>
                <td>Thời gian chuyến xe:</td>
                <td><span class="destination" id="duration"></span></td>
            </tr>
            <tr>
                <td>Khoảng cách:</td>
                <td><span class="destination" id="distance"></span></td>
            </tr>
            <tr>
                <td>Loại xe:</td>
                <td><span class="destination" id="type"></span></td>
            </tr>
            <tr>
                <td>Ngày đặt:</td>
                <td><span class="destination" id="dated"></span></td>
            </tr>
            <tr>
                <td>Hình thức thanh toán:</td>
                <td><span class="payment-form" id="result"></span></td>
            </tr>
            <tr>
                <td>Tổng tiền:</td>
                <td><span class="total-amount" id="totalFare"></span></td>
            </tr>
        </table>
        <div class="button-container">
            <div class="message-container">
                <div class="icon-container">
                    <i class="fa fa-comment"></i>
                </div>
            </div>
        </div>

        <div class="chat-popup" id="myForm">
            <div class="chat-header">
                <div class="chat-receiver">
                    <span id="PassengerImaged">
                        <img src="" alt="Receiver Image">
                    </span>
                    <span id="PassengerNames"></span>
                </div>
                <span class="close">&times;</span>
            </div>

            <div class="chat-content">
                <div class="chat-messages" id="chat-messages">
                    <div id="sender-chat-message"></div>
                    <div id="receiver-chat-message"></div>

                </div>
                <div class="chat-input-container">
                    <div class="chat-input">
                        <textarea id="chat-textarea" placeholder="Nhập tin nhắn..."></textarea>
                        <div class="send-btn-container">
                            <button id="send-message-btn"><i class="fa fa-paper-plane"></i></button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <button id="accept-ride" style="display:none;">Accept Ride</button>
        <button id="cancel-ride" style="display:none;">Cancel Ride</button>
        <div class="container">
            <div class="left">

                <div id="pickup">
                    <button id="Pickup-passenger">Đón khách</button>
                </div>
            </div>
        </div>
    </div>
    @*<div class="container">
            <div class="right">
                <h2>Dropoff</h2>
                <div id="dropoff">
                    <p>Destination:</p>
                    <input type="text" id="destination">
                    <p>Fare:</p>
                    <input type="text" id="fare">
                    <button onclick="dropoff()">Dropoff Passenger</button>
                </div>
            </div>
        </div>*@

    <script>
        $(function () {
            var bookingHub = $.connection.bookingHub;

            // Start the connection.
            $.connection.hub.start().done(function () {
                console.log('SignalR connection started for accepted!!!.');
                // Execute the script when the DOM is ready.
                $(document).ready(function () {
                    // Get the driver data.
                    var PhoneNumber = $("#phoneNumber").text();
                    var PhoneNumbers = $("#phone-number").text();
                    var DriverLocation = $("#DriverLocation").text();
                    var DriverName = $("#DriverName").text();

                    var DriverLicense = $("#DriverLicense").text();
                    var txtLocationPickup = $("#txtLocationPickup").text();
                    var txtLocationDrop = $("#txtLocationDrop").text();
                    var distance = $("#distance").text();
                    var duration = $("#duration").text();
                    var type = $("#type").text();
                    var dated = $("#dated").text();

                    var result = $("#result").text();
                    var totalFare = $("#totalFare").text();
                    var DriverRated = $("#DriverRated").text();
                    var DriverImages = $("#DriverImages").attr("src");

                    // Send the driver data to the server.
                    bookingHub.server.sendDriverData2(PhoneNumber, PhoneNumbers, DriverLocation, DriverName, DriverLicense, txtLocationPickup, txtLocationDrop, distance, duration, type, dated, result, totalFare, DriverRated, DriverImages);
                });
            });
        });

    </script>
    <script>
        $(function () {
    // Get the data from the session
            var PhoneNumber = "@Session["PhoneNumber"]";
            var DriverLocations = "@Session["DriverLocations"]";
            var DriverName = "@Session["DriverName"]";
            var DriverLicense = "@Session["DriverLicense"]";
            var txtLocationPickup = "@Session["txtLocationPickup"]";
            var txtLocationDrop = "@Session["txtLocationDrop"]";
            var result = "@Session["result"]";
            var totalFare = "@Session["totalFare"]";
            var PassengerPhone = "@Session["PassengerPhone"]";
            var distance = "@Session["distance"]";
            var duration = "@Session["duration"]";
            var type = "@Session["type"]";
            var dated = "@Session["dated"]";
            var DriverRate = '@Session["DriverRate"]';
            var DriverImage = "@Session["DriverImage"]";
            var PassengerImages = "@Session["PassengerImages"]";
            var PassengerName = "@Session["PassengerName"]";


             // Use the data as needed
            $('#DriverName').text(DriverName);
            $('#phoneNumber').text(PhoneNumber);
            $('#DriverLicense').text(DriverLicense);
            $('#DriverLocation').text(DriverLocations);
            $('#txtLocationPickup').text(txtLocationPickup);
            $('#txtLocationDrop').text(txtLocationDrop);
            $('#result').text(result);
            $('#totalFare').text(totalFare);
            $('#phone-number').text(PassengerPhone);
            $('#distance').text(distance);
            $('#duration').text(duration);
            $('#type').text(type);
            $('#dated').text(dated);
            $('#PassengerName').text(PassengerName);
            $('#PassengerNames').text(PassengerName);

            // Generate the appropriate star rating HTML based on the rating value
            var starHtml = "";
            for (var i = 0; i < DriverRate; i++) {
                starHtml += '<span class="fa fa-star"></span>';
            }
            for (var i = DriverRate; i < 5; i++) {
                starHtml += '<span class="fa fa-star-o"></span>';
            }
            // Set the star rating HTML in the NhanChuyenXe view
            $("#DriverRate").html(starHtml);
            // Set the image source in the HTML element
            $('#PassengerImages img').attr('src', '' + PassengerImages);
            $('#PassengerImaged img').attr('src', '' + PassengerImages);
        });

    </script>
    <script>
        $(document).on("click", ".fa-comment", function () {
            var phoneOfSender = $("#phoneNumber").text();
            var phoneOfReceiver = $("#phone-number").text();

            $.ajax({
                type: "POST",
                url: "/NhanTin/NoiDungNhanTin",
                data: { phoneOfSender: phoneOfSender, phoneOfReceiver: phoneOfReceiver },
                dataType: "json",
                success: function (data) {
                    $("#sender-chat-message").empty();
                    $("#receiver-chat-message").empty();

                    data.SenderChatMessages.forEach(function (message) {
                        $("#sender-chat-message").append("<div class='chat-message right'><div class='chat-message-text'>" + message + "</div></div>");
                    });

                    data.ReceiverChatMessages.forEach(function (message) {
                        $("#receiver-chat-message").append("<div class='chat-message left'><div class='chat-message-text'>" + message + "</div></div>");
                    });

                    // Open the chat popup
                    $("#myForm").show();
                },
                error: function (xhr, ajaxOptions, thrownError) {
                    console.log(thrownError);
                }
            });
        });
    </script>
    <script>
        $(document).ready(function () {
            // Initialize the SignalR connection
            var chatHub = $.connection.chatHub;

            // Start the SignalR connection
            $.connection.hub.start().done(function () {
                console.log('SignalR connection established.');

                // Handle send button click event
                $("#send-message-btn").click(function () {
                    sendMessage();
                });

                // Handle enter key press event
                $("#chat-textarea").keypress(function (event) {
                    if (event.which === 13 && !event.shiftKey) {
                        event.preventDefault();
                        sendMessage();
                    }
                });
            });

            // Function to send the message
            function sendMessage() {
                var message = $("#chat-textarea").val();
                if (message.trim() !== "") {
                    // Append the message to the sender's chat messages
                    var senderChatMessage = $("<div class='chat-message right'><div class='chat-message-text'>" + message + "</div></div>");
                    $("#sender-chat-message").append(senderChatMessage);

                    // Clear the input textarea
                    $("#chat-textarea").val("");

                    // Send the message via AJAX
                    $.ajax({
                        url: "/NhanTin/ThemNhanTin",
                        type: "POST",
                        dataType: "json",
                        data: {
                            message: message,
                            phoneOfSender: $("#phoneNumber").text(),
                            phoneOfReceiver: $("#phone-number").text()
                        },
                        success: function (response) {
                            // Handle success response if needed
                        },
                        error: function (error) {
                            console.log(error);
                        }
                    });

                    // Send the message via SignalR
                    var phoneOfSender = $("#phoneNumber").text();
                    var phoneOfReceiver = $("#phone-number").text();
                    chatHub.server.driverSend(message, phoneOfSender, phoneOfReceiver);
                }
            }
        });

    </script>
    @*<script>
            //$(document).ready(function () {
                // Create a connection to the SignalR hub
                var chatHub = $.connection.chatHub;

                // Define a client-side method to receive messages
                chatHub.client.addMessage = function (message) {
                    // Append the received message to the appropriate container
                    var chatMessage = $('<div class="chat-message left"><div class="chat-message-text">' + message + '</div></div>');
                    $('#receiver-chat-message').append(chatMessage);
                };

                // Start the SignalR connection
                $.connection.hub.start();

                // Rest of your code...
            });

        </script>*@

    <script>
        $(function () {
            // Initialize the connection to the SignalR hub
            var chatHub = $.connection.chatHub;

            // Define a client-side method to receive the message from the server
            chatHub.client.displayCustomer = function (message, phoneOfSender, phoneOfReceiver) {
                console.log(message); // Log the message to the console
                var chatMessage = $('<div class="chat-message left"><div class="chat-message-text">' + message + '</div></div>');
                $('#receiver-chat-message').append(chatMessage);
            };


            // Start the SignalR connection
            $.connection.hub.start();
        });

    </script>
    <script>
        $(document).ready(function () {
            $("#Pickup-passenger").click(function () {
                // Send the announcement to NhanChuyenXe.cshtml using SignalR
                $.connection.bookingHub.server.sendAnnounce();
            });
        });
    </script>
    @*<script>
        //$(document).ready(function () {
            // Listen for the cancelPickupRequest event from SignalR
            $.connection.bookingHub.client.cancelPickupRequest = function () {
                // Display the cancellation notification using a library of your choice (e.g., SweetAlert2)
                Swal.fire({
                    title: 'Announcement',
                    text: 'The passenger has canceled the pickup request.',
                    icon: 'warning',
                    confirmButtonText: 'OK'
                }).then(() => {
                    // Redirect to a new page or perform any other required actions
                    window.location.href = "@Url.Action("DonKhach", "TaiXe")";
                });
            };

            // Start the SignalR connection
            $.connection.hub.start();
        });
    </script>*@
    <script>
        $(function () {
            var bookingHub = $.connection.bookingHub;
            bookingHub.client.updateRequestStatus = function (status) {
                            if (status === "accepted") {
                                swal({
                                    title: "Đã chấp nhận đón khách",
                                    text: "Đón khách hàng!. ",
                                    icon: "success",
                                    buttons: false,
                                    timer: 2000
                                });
                                // Redirect to the new page
                                window.location.href = "@Url.Action("DonKhach", "TaiXe")";
                            } else if (status === "cancelled") {
                                swal({
                                    title: "Hủy xác nhận đón khách",
                                    text: "Hủy đón khách!",
                                    icon: "error",
                                    buttons: false,
                                    timer: 2000
                                });
                            }
                        };
        });
    </script>
</body>
</html>


